# 本地配置文件（含私密 webhook 等，请勿提交）
config.json

# 运行时中间产物
web-articles/
debug-snapshots/
debug-shots/
*.log

# 运行时日志（签到结果、书签同步日志、自迭代历史，每次运行动态生成）
signin-log.json
sync-log.json
iterations.json

# 临时验证脚本
.tmp-*.ps1

# Trae 工作目录
.trae/
.uploads/

# 操作系统
Thumbs.db
.DS_Store
Desktop.ini

# IDE
.vscode/
.idea/
*.suo
*.user

# 项目内部 AI 工作记忆（不公开）
.workbuddy/

# 运行时签到结果 / 生成报告（每次运行动态生成，由 gen-report.py 读取 rerun-cumulative.json 生成）
rerun-cumulative.json
signin-report-*.html

# 临时诊断脚本
diag-*.ps1

# Python 运行缓存
__pycache__/
*.pyc